#!/bin/sh
#
# This shows how many unseen episodes a user has on
# BetaSeries

source /usr/libexec/byobu/include/shutil
count="$(bscli -p watchlist | wc -l)"
if [ "$count" != "0" ]; then
	color b k b; echo "B$(bscli -p watchlist | wc -l)"; color --
fi
# vi: syntax=sh ts=4 noexpandtab
